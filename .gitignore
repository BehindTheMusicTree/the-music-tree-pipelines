# Repo Python venv
.venv/

# editable pip install -e ".[dev]"
*.egg-info/
__pycache__/
*.py[cod]

.DS_Store

# Local secrets / DB connection
.env

# MusicBrainz dumps and downloaded Wikidata extracts (local, disposable)
data/
*.dump
*.sql.gz

# Pipeline outputs (bronze/silver parquet)
bronze/
silver/

# Local scratch files (e.g. generated PR descriptions)
.local/
.github/pr-descriptions/

# Coverage reports
.coverage
coverage.xml
